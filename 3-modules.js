// Modules
// CommonJS defines each file as a module by default
// Modules encapsulate code
const names = require('./4-names')
const utils = require('./5-utils')
const data = require('./6-alternativeImport')
require('./7-mindGrenade')
//function as constant is defined rather than function definition
// utils(names.susan)
// utils(names.peter)
// utils(names.john)